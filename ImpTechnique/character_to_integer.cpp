#include <iostream>

int main() {
    int digit = 5;           // Input digit
    char ch = '0' + digit;   // Convert digit to char

    std::cout << "The character representation of digit " << digit << " is: " << ch << std::endl;
    return 0;
}
